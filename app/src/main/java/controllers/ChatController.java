package controllers;

import com.example.jab.ChatActivity;
import com.google.firebase.auth.FirebaseAuth;

public class ChatController {
    public ChatController(FirebaseAuth auth, ChatActivity chatActivity) {
    }
}
